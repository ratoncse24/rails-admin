<div class="box">
  <div class="box-header">
    <h3 class="box-title">Sliders </h3>
    <div class="pull-right">
      <%= link_to "Add New", new_slider_path, :class => "btn btn-primary"  %>
    </div>

    <div class="row">
      <% if notice %>
        <div class="col-md-6 col-md-offset-3">
          <div class="alert alert-success alert-dismissible">
            <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
            <h4><i class="icon fa fa-check"></i> Message!</h4>
            <%= notice %>
          </div>
        </div>
      <% end %>
    </div>


  </div>
  <!-- /.box-header -->
  <div class="box-body">

    <table id="example1" class="table table-bordered table-striped">
      <thead>
      <tr>
        <th>Title</th>
        <th>Status</th>
        <th>Description</th>
        <th>Image</th>
        <th>Action</th>
      </tr>
      </thead>
      <tbody>
      <% @sliders.each do |slider| %>

        <tr>
          <td><%= slider.title %></td>
          <td>
            <% if slider.status == 1 %>
              <span class="label label-success">Active</span>
            <% else %>
              <span class="label label-warning">DeActive</span>
            <% end %>

          </td>
          <td><%= slider.description %></td>
          <td>
            <% if slider.image.attached? %>
              <%= image_tag slider.image.variant(resize: '100x50!').processed %>
            <% end %>

          </td>
          <td>

            <%= link_to edit_slider_path(slider) , :class=> "btn btn-success" do %>
              <%= fa_icon "edit" %>
            <% end %>

            <%= link_to slider, method: :delete , data: { confirm: "Are you sure ? "} ,:class =>"btn btn-danger" do %>
              <%= fa_icon "trash" %>
            <% end %>

          </td>
        </tr>

      <% end %>


      </tbody>
    </table>
  </div>
</div>